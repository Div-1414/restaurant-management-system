[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && python manage.py collectstatic --noinput && daphne -b 0.0.0.0 -p $PORT restaurant_project.asgi:application"
healthcheckPath = "/admin/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"